# AI霖子 Obsidian 插件开发指令

修改本仓库前，必须完整阅读并遵守 [`docs/开发上下文.md`](docs/开发上下文.md)。

不可放宽的核心边界：公开插件只做界面、本地笔记操作和版本化 API 调用；核心提示词、AI 判断、计费、学员数据和模型编排只存在于私有后端。插件会话使用 `obsidian:` 命名空间：插件对话可以同步到云端并被网页版查看，但网页端和微信端对话不得同步回插件，也不得被插件清空。

涉及对话历史、恢复、删除、跨端同步或鉴权的改动，必须同时验证插件端与服务端隔离测试。

官方市场版本不得包含插件自更新器。版本发布必须使用与 `manifest.json` 完全一致的纯版本号 GitHub Release，安装与更新交给 Obsidian 官方机制；手动测试包仅作为审核通过前的过渡分发方式。

主对话的「带上当前笔记」和「智能搜索 Vault」默认都必须关闭，新建或切换对话时恢复用户设置的默认值。只有用户明确要求给当前笔记/文章的某个 Part 或段落新增配图时，才读取当前笔记并走私有后端的单图接口；仅勾选开关不能自动把自由生图改道成文章配图。

通用 AI 生图必须作为主对话中的显式模式提供：支持连续改图，也允许用户关闭「参考上一张图」后开始新图；用户点击「继续修改这张」时，上一张图必须作为主画布，完整保留未要求修改的主体、构图、比例、风格、文字和二维码位置，不得因当前笔记开关仍开启而套用公众号配图、手绘流程图或固定信息卡模板。成功结果自动保存到用户 Vault，并在本地对话中保留图片卡片。云端历史只同步文字，不同步图片数据或本地路径。

公众号配图的专属人偶参考图必须保存在用户 Vault；普通设置只存相对路径，只有用户主动生图时才发送本次所需的压缩图。整篇配图、续跑、对话补图与单图修改必须沿用该设置，参考图丢失则回退通用人偶。

主对话“AI 生图模式”和“我的专属人偶”只对 Pro/Business 开放；通用人偶文章配图保持原有权益。会员权限必须由私有服务端强制复核，公开插件只能做界面提示和提前拦截。

主对话图片附件只对 Pro/Business 开放：插件可从 Vault 或电脑选择最多 3 张 PNG/JPG/WebP，在本地压缩后只随下一条消息发送；图片数据不得写入本机或云端对话历史。公开插件只负责选择、压缩与请求封装，类型/数量/大小/会员权限和视觉模型路由必须由私有后端再次校验。

Vault 智能搜索与多笔记整篇授权必须分开：智能搜索默认可在用户电脑本地扫描 Markdown、TXT、可复制文字的 PDF 和 DOCX，并只发送最多 8 条、单条 4,000 字、合计 20,000 字的相关片段，不持久化索引、不上传整个 Vault；扫描 PDF 不做 OCR，旧版 DOC 不解析。点号开头的隐藏系统目录、废纸篓与 `AGENTS.md`/`CLAUDE.md`/`_sub-agent-summaries.md` 开发辅助文件在读取前跳过;除此之外用户 Vault 的全部内容(包括文件名含「㊙️」的笔记)都参与检索与管理——这是有意设计(2026-07-30 Alina 确认:插件服务学员自己的数据,「㊙️ 跳过」只属于 Alina 自己大脑的云端同步场景,与本插件无关);「用户设置的排除文件夹」已于 v0.6.25 移除。多笔记与文件夹整篇授权仍只对 Pro/Business 开放，只有用户明确勾选并确认的正文才发送。自动搜索上限与主动授权上限都必须由私有服务端再次校验。

自 v0.7.0 起，开启「智能搜索 Vault」后可进入模型无关的本机工具循环：服务端模型只能请求 `vault_search`、`list_folder`、`read_note`，插件在本机校验后执行，最多 6 轮、每轮 4 次；模型永远没有直接磁盘权限。工具结果与本地路径不得写入云端聊天历史。整理/移动/重命名必须先返回结构化方案，插件二次确认后才执行；只允许新建文件夹、移动和重命名，禁止删除、禁止覆盖、禁止改扩展名，隐藏目录、回收站、开发辅助文件和本地 Skills 根目录受保护。执行日志只存在 Obsidian `data.json`，移动/重命名支持撤销；撤销不删除新建的空文件夹。
自 v0.7.1 起，Agent 模式不得预跑旧的一次性 top-snippet 搜索；只在模型明确请求 `vault_search/list_folder/read_note` 后按需读取。列目录必须走 `list_folder`，读取精确文件必须走 `read_note`；摘要连同截断标记都不得突破服务端字符上限。
自 v0.7.2 起，模型给出的工具调用 ID 由插件按轮次和序号重新命名后再累计回传，不能依赖模型在多轮间维持唯一 ID；服务端继续拒绝真正重复或伪造的工具结果。
自 v0.7.3 起，客户端显式区分 `answer/organize` 意图。整理请求没有工具调用或结构化计划时不得当成完成；客户端会进入下一轮协议纠正，直到得到可确认计划或触发安全轮次上限。

本地 Skill 根目录由用户在 Vault 内配置，默认 `system/skills`，必须整体排除在普通 Vault 搜索之外。对话创建 Skill 时，模型不得指定路径；插件只接受标准小写 kebab-case 名称，固定写入 `<配置目录>/<name>/SKILL.md`，用户点击确认后才新建且绝不覆盖。新生成文件的 YAML 只含 Agent Skills 公共字段 `name`、`description`，AI霖子扩展写在 Markdown 正文中。
