# AI霖子 Obsidian 插件开发指令

修改本仓库前，必须完整阅读并遵守 [`docs/开发上下文.md`](docs/开发上下文.md)。

不可放宽的核心边界：公开插件只做界面、本地笔记操作和版本化 API 调用；核心提示词、AI 判断、计费、学员数据和模型编排只存在于私有后端。插件会话使用 `obsidian:` 命名空间：插件对话可以同步到云端并被网页版查看，但网页端和微信端对话不得同步回插件，也不得被插件清空。

涉及对话历史、恢复、删除、跨端同步或鉴权的改动，必须同时验证插件端与服务端隔离测试。

官方市场版本不得包含插件自更新器。版本发布必须使用与 `manifest.json` 完全一致的纯版本号 GitHub Release，安装与更新交给 Obsidian 官方机制；手动测试包仅作为审核通过前的过渡分发方式。

发布候选在完整自动化测试和独立测试 Vault 真机验收通过前，只能保留在本地，不得推送 GitHub。只要插件版本代码推送到 GitHub，就必须在同一轮完成纯版本号 tag、GitHub Release、WebApp `LATEST_PLUGIN_VERSION` 与生产下载包核验；若插件尚未进入 Obsidian 官方社区目录，还必须提交官方审核。禁止停在用户看到更新提示却没有可安装 Release 的中间状态。

v0.7.18 起主对话不再显示「带上当前笔记」勾选项。用户文字明确要求处理“当前/这篇”笔记时，插件只读取当时激活且仍然打开的一篇 Markdown；关闭标签页立即撤销授权，普通闲聊不读笔记。v0.7.30 起由 Luna 在适合的纯文字主对话中决定是否请求本机工具。在模型真正发起 `vault_search/list_folder/read_note` 前，插件不得读取、建立正文索引或发送任何 Vault 内容；普通闲聊必须零正文读取与零工具调用。第一次真正的文件工具调用可激活本机轻量索引，之后依靠 Vault 事件增量更新。多笔记/文件夹整篇授权仍必须由用户明确勾选。

“客户咨询简报”是 v0.7.18 的内置技能；v0.7.19 起与“销售复盘”统一先让用户从 Vault 明确选择并锁定一份 MD、TXT、可复制文字的 PDF 或 DOCX 逐字稿。文件解析只发生在本机，旧版 DOC 与扫描版 PDF 不解析；插件只发送提取后的这一份逐字稿和必要表单字段，不读取知识库或其他 Vault 文件。两个技能继续使用既有 WebApp 数据库提示词、模型、权限、限流、积分与用量记录；原始文件不上传，逐字稿不写 AI霖子数据库。“客户咨询简报”仍只把服务端 Markdown 临时渲染成与 WebApp 同视觉的单张 PNG 长图，最终只保存 `AI霖子输出/客户咨询简报/*.png`，不落 Markdown/Word，不包含私有提示词或模型配置。

通用 AI 生图只在主对话中以自然语言提供，不得再显示独立生图模式、比例下拉框或「参考上一张图」开关。用户明确要求生成或修改图片时，私有后端自行判断是否出图、张数、用途和画幅；用户明确指定比例优先，否则公众号头条封面用 2.35:1（最终 900×383）、小红书封面/卡片用 3:4、普通横版用 16:9、方图用 1:1。用户点击「继续修改这张」或点名“修改第 N 张”时，目标图必须作为主画布；未明确改比例时保留原画布，明确改成另一平台画幅时才重排。成功结果自动保存到用户 Vault，并在本地对话中保留图片卡片。云端历史只同步文字，不同步图片数据、本地路径或图片任务协议。

公众号配图的专属人偶参考图必须保存在用户 Vault；普通设置只存相对路径，只有用户主动生图时才发送本次所需的压缩图。整篇配图、续跑、对话补图与单图修改必须沿用该设置，参考图丢失则回退通用人偶。

主对话“AI 生图”和“我的专属人偶”只对 Pro/Business 开放；通用人偶文章配图保持原有权益。会员权限必须由私有服务端强制复核，公开插件只能做界面提示和提前拦截。

主对话图片附件只对 Pro/Business 开放：插件可从 Vault 或电脑选择最多 3 张 PNG/JPG/WebP，在本地压缩后只随下一条消息发送；图片数据不得写入本机或云端对话历史。公开插件只负责选择、压缩与请求封装，类型/数量/大小/会员权限和视觉模型路由必须由私有后端再次校验。

Vault 按需搜索与多笔记整篇授权必须分开：只有明确的 Vault 文件任务才允许本机工具扫描 Markdown、TXT、HTML、PPTX、XLSX、可复制文字的 PDF 和 DOCX，并只发送回答所需的受限结果，不上传整个 Vault。第一次真实文件检索可在 IndexedDB 中持久化路径、mtime、size、索引状态与紧凑词项 Bloom 位图，不存原文；命中后必须从 Vault 重读少量候选正文。既有索引在插件重启后继续通过 `create/modify/delete/rename` 事件低速增量更新；尚未使用检索的新 Vault 在启动时不读正文。扫描 PDF 不做 OCR，旧版 DOC/PPT 不解析。`list_folder` 列出全部文件类型，不可读类型以 `readable:false` 标注；逐字稿技能的候选仍固定为 MD/TXT/PDF/DOCX。点号开头的隐藏系统目录、废纸篓与 `AGENTS.md`/`CLAUDE.md`/`_sub-agent-summaries.md` 开发辅助文件在读取前跳过；除此之外用户 Vault 的全部内容（包括文件名含「㊙️」的笔记）都参与检索与管理。多笔记与文件夹整篇授权仍只对 Pro/Business 开放，只有用户明确勾选并确认的正文才发送。搜索结果与主动授权上限都必须由私有服务端再次校验。

自 v0.7.0 起 Vault 文件任务可进入模型无关的本机工具循环；v0.7.17 起由明确的 Vault 对话意图自动触发，不再依赖开关。服务端模型只能请求 `vault_search`、`list_folder`、`read_note`，插件在本机校验后执行，最多 12 轮、每轮 4 次（v0.7.45 起由 6 轮放开；本机结果总预算 36 万字符，服务端硬上限 40 万）；模型永远没有直接磁盘权限。工具结果与本地路径不得写入云端聊天历史。整理/移动/重命名必须先返回结构化方案，插件二次确认后才执行；默认只允许新建文件夹、移动和重命名，禁止覆盖、禁止改扩展名，隐藏目录、回收站、开发辅助文件和本地 Skills 根目录受保护。v0.7.22 起仅在用户明确要求删除时增加 `trash_note`：一份计划只能包含一篇现有 `.md` 笔记，必须再次确认，只调用 Obsidian 回收站接口，禁止附件、文件夹、批量删除和永久删除；恢复须到系统废纸篓/回收站或 Obsidian `.trash`。v0.7.42 起删除范围放开：仍必须由用户当轮明确说删除、纯删除单独成卡（不与移动/新建/写入混排）、确认卡逐条列出目标并再次确认、只走回收站接口且永远禁止永久删除，但允许批量（单卡随方案上限最多 60 项）、任意文件类型和文件夹；文件夹整夹删除前本机校验不得裹挟受保护路径（本地 Skills 根目录、开发辅助文件等），嵌套或重复目标整单拒绝；服务端指令按 0.7.42 版本门禁分叉，旧插件仍收到「单篇 .md」旧协议。v0.7.23 起用户明确删除“当前/这篇笔记”时，客户端直接使用消息发送瞬间锁定的 `noteContext.path` 生成待确认方案，不消耗模型轮次、不允许后来切换的标签页重定向目标；v0.7.24 把删除/回收动作纳入当前笔记显式授权识别，确保该路径实际可用；v0.7.25 让每张确认卡只读取自己保存的执行记录，未执行的新方案不得借用历史记录隐藏确认按钮。仍必须经过确认卡和再次确认。执行日志只存在 Obsidian `data.json`，移动/重命名支持插件内撤销；撤销不删除新建的空文件夹。
v0.7.26 起主对话把“咨询交付逐字稿、交付顾问咨询逐字稿、销售/谈单逐字稿、咨询记录、客户/学员档案”等业务词视为本地文件对象；“搜索/扫描/读取/处理”必须当轮真正进入本机工具循环。客户端若还没有任何真实工具结果，必须以 `missing_tool_use` 纠正，禁止用“我现在先扫描”或猜测路径结束；模型第一次返回的方案不完整、越界或含未知操作时以 `invalid_plan` 在安全轮次内自动纠正，不能直接把格式错误甩给用户。模型偶发空响应时以 `empty_response` 自动续跑；只读轮误给写入计划时以 `unexpected_plan` 拒绝并纠正，安全轮次最后仍保留可见只读正文。用户明确“先不要写入/只生成草稿”必须保持只读处理；“输出一个可以直接写入/追加的 Markdown 草稿或章节”也只是内容产出，不得当成执行授权。只有“请把这段追加到某个准确路径”等明确动作才进入写入确认。只有一份逐字稿直接分段读完，多份且未说“最新”先列准确文件名让用户选择，明确“最新”才按 `modifiedAt` 选择。主对话确认后的跨文件 Markdown 写入增加 `create_note/append_note/update_note/replace_note`：每次方案只能锁定一个准确路径，已有目标先 `read_note`，完整展示新增/替换正文或逐字局部差异，再二次确认；目标的 path/mtime/size 在方案生成时锁定，确认前变化即停止，同名新建、保护路径、附件、混合写入和静默覆盖全部拒绝。整篇替换仅在用户明确要求时使用并保留 frontmatter；回滚依赖 Obsidian 撤销/文件恢复。v0.7.28 起 `update_note` 可带完整 `frontmatter.old/new`，确认卡前必须本机 dry-run、解析 YAML 并按可选 Skill 模板校验字段与章节；失败回到工具循环重做，不展示不可执行按钮。直接调用“销售复盘/客户咨询简报”继续要求用户显式选择并锁定一份逐字稿，不因主对话自动搜索而放宽隐私边界。
v0.7.26 起普通 AI 回复下不再常驻“存为笔记/更新当前笔记”，输入区也不常驻“存入知识库”；用户直接说“存成新笔记”“用刚才内容覆盖当前笔记”“把当前笔记存入 AI霖子知识库”后才进入对应确认流程。知识库动作在发送瞬间锁定仍打开的当前 Markdown，后续切页不能换来源。用户明确说“请记住/存入事实记忆”时调用服务端事实记忆写入接口，只有数据库实际新增或更新后才允许回复成功；主体不属于当前用户、证据不足或服务失败都必须如实说明。事实记忆与“AI霖子知识库”是两个不同目标，文案和 API 不得混用。
自 v0.7.1 起，Agent 模式不得预跑旧的一次性 top-snippet 搜索；只在模型明确请求 `vault_search/list_folder/read_note` 后按需读取。列目录必须走 `list_folder`，读取精确文件必须走 `read_note`；摘要连同截断标记都不得突破服务端字符上限。
自 v0.7.2 起，模型给出的工具调用 ID 由插件按轮次和序号重新命名后再累计回传，不能依赖模型在多轮间维持唯一 ID；服务端继续拒绝真正重复或伪造的工具结果。
自 v0.7.3 起，客户端显式区分 `answer/organize` 意图。整理请求没有工具调用或结构化计划时不得当成完成；客户端会进入下一轮协议纠正，直到得到可确认计划或触发安全轮次上限。
自 v0.7.8 起，只读问答同样执行完成校验：模型回复“等我/稍后处理完再回答”或统计问题没有数字时不得结束，客户端会自动续跑并把纠正原因传给私有后端。咨询场次问题属于本机确定性统计例外，可在首轮全量扫描咨询时间线/汇总或逐字稿文件并去重，不预传普通 top-snippet；`vault_search` 的 `fact` 必须回传，不能丢弃。私教场次按带日期的明细中明确含“私教”的记录统计，全部咨询才使用总表总计，不能混淆口径。`list_folder` 支持最深 12 层、20,000 条本机扫描与 offset 分页，并返回 totalFiles/totalFolders/totalEntries，模型不得把当前页当全量。达到 12 轮仍未完成时必须明确失败（v0.7.45 起预算用尽改为带着已有成果优雅收尾，用户说「继续」可接续），绝不能留下不存在的后台任务承诺。
自 v0.7.9 起，咨询统计识别省略年份的月份（如“8月份”按设备当前年份）、本月、上月、今年与去年；设备时间只用于本机解析，不上传。咨询统计一旦产生本机确定性 `fact`，插件直接用该事实回答，不再让主模型改写或继续核对，也不产生主对话模型费用。普通搜索结果没有 `fact` 时绝不能走本机直答。回复任意段落出现“继续核对…再去重”等未完成承诺仍必须拒绝结束。
自 v0.7.10 起，本地 Skill 入口超过 12,000 字不再直接报错：首轮只发送入口前段，模型必须通过 `read_skill_file` 在本机按需继续读取入口、当前 Skill 的 `references/`/`scripts/`，以及入口中明确引用的 Vault SOP。该工具只读，不会执行脚本；文件路径和正文不写入云端历史。受控本地执行器与 Windows/macOS 真人验收为后续独立阶段，v0.7.10 不得宣称已能运行 Skill 脚本。
自 v0.7.11 起，本地程序执行默认关闭，用户在设置中主动开启后，模型只能提出结构化 `propose_skill_action`，每轮一个且必须由用户逐次确认。程序仅开放 Node.js/Python/FFmpeg/FFprobe，禁止 Shell、PowerShell、`node -e`、`python -c/-m`、npm/npx、URL/管道输入和静默覆盖；路径必须使用 `$SKILL/$VAULT/$OUTPUT/$TEMP`。Node/Python 只允许当前 Skill 内已授权脚本。终端输出默认不交给 AI；只有动作明确声明并在确认弹窗展示后，最多 4,000 字可临时回传模型，仍不写云端对话历史。执行日志仅保存动作元数据，不保存命令输出和本机绝对路径。已声明且实际新生成的 Vault 文件可移到系统废纸篓/回收站；若生成后被修改则拒绝撤销。公开 UI 必须明确提示：脚本与 Obsidian 进程同权限，只运行可信 Skill，不得宣传为绝对沙箱。macOS 本机验收不等于 Windows 真人验收。
自 v0.7.12 起，普通主对话中的明确图片生成/修改请求可自动调用 AI 生图；小红书卡片、海报、长图和普通封面不得误导到“文章配图”。单次最多 6 张，协议块格式损坏时不执行；多图批次保留编号，“修改第 N 张”必须精确选中最近一批的对应图片。公开插件不显示、不固化具体图片模型；模型选择、计费和提示词编排仍只存在私有后端。
自 v0.7.14 起，知识库目录由 Luna 按用户场景动态规划，不得在公开插件写死唯一模板。用户点名“一人公司驾驶舱/AI霖子默认知识库”时，服务端默认建议 `000_Inbox/01_Raw/02_Wiki/03_Dashboard/04_Output/05_System/06_Archive`，并包含 `05_System/Skills` 子目录；用户可在点击前多轮修改。插件以结构化确认卡展示目录及 `inbox/sources/knowledge/output/localSkills` 绑定，允许“仅创建”或“创建并应用”；用户确认前绝不落盘，已有目录跳过，不移动/覆盖/删除现有内容。目录规划轮次必须隔离 addTask/CRM 云端写工具，不能再把创建目录误记成任务。新安装默认映射为编号目录；旧用户设置不静默迁移。

本地 Skill 根目录由用户在 Vault 内配置，新安装默认 `05_System/Skills`（旧用户自定义或旧默认不静默迁移），必须整体排除在普通 Vault 搜索之外。对话创建 Skill 时，模型不得指定根路径；插件只接受标准小写 kebab-case 名称。v0.7.28 起可在一次确认中只新建 `<配置目录>/<name>/SKILL.md` 及 `references/scripts/assets` 下受限的普通文本文件，任何越界、重复、二进制或同名现有文件都整包拒绝。新生成 SKILL.md 的 YAML 只含 Agent Skills 公共字段 `name`、`description`，AI霖子扩展写在 Markdown 正文中。

用户在主对话明确说“修改/更新/优化 + 已安装 Skill”时，必须先锁定现有 `SKILL.md` 路径，不得进入同名新建通道。AI 可通过同一主对话交互生成更新方案，但公开插件仍只接收结构化变更：必须展示所有文本改动与删除，用户只确认一次，确认前复核全树指纹。插件不建立 Skill 历史版本；写入中途失败只用本轮内存原字节自动回滚，遇到用户并发编辑必须保留现场并提示使用 Obsidian 文件恢复/备份。旧 `ai-linzi-versions` 只排除于正式文件树，不再读写或自动删除。

当前确认口径覆盖上文历史版本叙述：普通新建/更新 Markdown、移动整理、创建 Skill、生成一次性成品和动态工作台，必须先展示完整可执行预览；用户点击预览卡上的执行按钮就是唯一一次确认，不得再弹第二层确认窗。回收站/删除仍保留预览卡后的独立危险确认；本地程序执行、CRM/外部系统写入继续按各自高风险边界单独确认。自然语言表达“把这套做法固定/固化/封装成以后反复使用的工作流”时，即使没写 `Skill` 也必须进入 Skill Creator；模型返回的任何创建 Skill 协议块都要经过同一整包校验，残缺包不得出现创建按钮。

v0.7.21 起用户界面统一使用“我的 Skills”，旧的“本地 Skills”只保留为对话兼容口令。v0.7.28 起 Skill 可在正文“AI霖子自动调用”章节主动列出带动作的完整短语；客户端只做确定性匹配，不按 description 猜语义，未声明时仍必须显式调用。AI霖子自身不加载 Vault 根 `AGENTS.md`；该文件继续从普通搜索、整理和移动中排除，避免与 Codex/Claude Code 等其他客户端的工作规则混淆。

v0.7.29 起，同一对话里已经实际调用过的本地 Skill 可在“继续、处理最新一份、写入客户档案”等明确续跑语句中按已锁定的 `SKILL.md` 路径继续；只保存入口路径，不保存 Skill 正文，也不得把路径上传云端。确认新建/更新一篇客户档案后，插件可以提示“查看 CRM 同步差异”，但不能自动同步：再次点击后只读取这一篇档案，按客户编号→微信号→称呼匹配，重名或回收站占号必须停止；完整展示字段差异并二次确认后才调用 `/api/plugin/v1/customers/sync`。来源逐字稿不得上传，完整客户档案正文由用户在确认窗单独选择；同步成功前不得显示成功。
v0.7.22 起所有读取当前笔记的耗时技能必须在启动时锁定来源文件和正文；用户随后切换、浏览或打开其他笔记不得改变正在执行的任务。只有仍停留在“是否使用当前笔记”授权弹窗的选题雷达会在弹窗打开时锁定文件、确认后再读取该锁定文件；文件已被实际移除时必须中止并提示，不能回退到新的当前笔记。

改面板 CSS 前必须先看这两条铁律，它们只有在真机 Obsidian 里才会暴露，本地渲染台和单元测试都抓不到，各自已配守卫测试（`scripts/test-button-feedback.mjs` 第 3.5、3.6 组，改坏必报错）。第一条：面板内自定义按钮的选择器权重必须达到 (0,3,1)，写成 `.ai-linzi-root <祖先类> button.ai-linzi-x`，不能只写单类或省略 `button` 元素选择器。原因是 Obsidian 与各家主题普遍带 `button { padding; border-radius; background }` 一类规则，深色主题常写成 `.theme-dark button`（0,1,1）甚至更高；0.7.71 实测单类写法下发送键的 `border-radius` 被主题改成 5px、`padding` 改成 4px 10px，圆形发送键在真机上根本不是圆的，深色主题下三处按钮的透明底也被全部刷回灰底。官方市场检查禁用 important 声明，选择器权重是唯一可用的杠杆；`@container` 不改变权重，其内部规则同样要写足。第二条：权重只能覆盖你显式声明过的属性。主题的 `button` 规则里还有 `height`、`white-space`、`line-height`、`font-family`，凡是我们没写的那几条，权重再高也拦不住——0.7.71 第二轮真机报障就是起手式卡被主题的固定 30px 高度压扁、第二行说明文字溢出卡片并被下一张卡遮住。因此所有由内容撑高的按钮必须集中声明一条共享 reset：`height: auto; min-height: 0; max-height: none; white-space: normal; line-height: <具体值>; font-family: inherit`。

侧边栏面板的响应式断点必须用 `@container` 而不是 `@media`。Obsidian 的侧边栏是宽窗口里的一根窄栏，`@media (max-width: …)` 判定的是窗口宽度，320px 宽的面板会拿到 1400px 视口的样式，窄屏规则永远不命中。`.ai-linzi-root` 已声明 `container-type: inline-size; container-name: ai-linzi-panel`，新增断点一律写 `@container ai-linzi-panel (max-width: …)`。收窄时只允许隐藏可见文字，`aria-label` 与 `title` 必须始终保留，不得出现一排没有说明的图标。

新增任何第三方依赖前，必须先扫描其 dist 产物是否内联了动态 `<script>` 创建或 `eval`/`new Function` 类 polyfill——官方市场审核的 CODE OBFUSCATION 区按发布产物里的字面量计数，2026-08 的下架事故就是 `docx` 依赖链带进 4 处动态 `<script>` 所致。`npm run check:marketplace` 已内置两道产物闸：构建产物 `main.js` 的动态 `<script>` 字面量必须为 0；动态代码生成锁定基线 4 处（全部来自第三方，本仓库源码硬禁 eval 与 new Function），只许降不许升，涨了必须定位来源而不是默默放大基线。

抬 `LATEST_PLUGIN_VERSION` 时，先在 webapp 仓库 grep 全库的旧版本号字面量找齐所有钉子——该常量目前在 `scripts/test-plugin-v1-contract.mts` 与 `scripts/test-plugin-vault-agent.mts` 两处都有写死断言（刻意如此，逼开发者两处刻意改），只改一处会让 CI 红叉。推送前按 `.github/workflows/ci.yml` 在本地跑完整四步：`npx tsc --noEmit`、`npm run lint -- --max-warnings=0`、`npm run test:plugin-api`、`npm run test:stability`，不要只跑自己记得住的那几个测试。
