# AI霖子 0.7.69 候选修复与真机验收

日期：2026-08-20（2026-08-21 凌晨续验收）
状态：本机候选；后端修复已发布，插件尚未发布

## 结论

0.7.69 的代码、市场检查和本地发布包已经通过自动化门禁；Skill 创建、客户档案、CRM、跟进任务、客户咨询简报 PNG 和经营周报 HTML 均已有真实数字大脑证据。WebApp `f250794` 已进入生产，咨询简报后端空响应问题已解除。

插件本身仍未发布。2026-08-21 凌晨最后重跑 Studio 自建「7 天行动计划」Skill 时，当前演示密钥有效，但演示账户余额为 19 分，低于主对话 20 分安全发起门槛，后端按设计返回 402。这不是密钥、权限或 Skill 路由缺陷；未经 Alina 明确授权不得给账户赠送积分，也不得降低生产计费保护。Mac 随后锁屏，电脑操作工具按安全规则停止。因此还缺「余额充足 + 解锁后」这一条真人现场试运行回执，不能把自动化测试表述为 Alina 本人验收。

## 已修复

- Skill Creator：`skillVersion` 对象版自动规范为三段字符串；提示词同时禁止对象格式。
- Skill Studio：真实用户话术、积分说明、自然语言调用说明、真实业务示例。
- Skill 目录：安装、对话创建和 ZIP 导入继续跟随用户设置，不写死开发机路径。
- 咨询闭环：模板优先、父目录核验、缺少子目录时的受控创建、CRM 真实回执、任务专用云端执行轮、失败重试与成功防重复。
- 经营周报：固定快照分页、排除整个输出目录和 Skills、mtime 风险提示、单页驾驶舱语义渲染。
- 市场边界：无本机脚本、无模型直连、无新增远程依赖；本地写入继续需要确认，SecretStorage 设置未改。

## 真机证据

测试仓库：`/Users/alinawang/Desktop/数字大脑`

- 当前候选：0.7.69；只覆盖 `main.js/manifest.json/styles.css`，原 `data.json` 与密钥未改。
- 官方 Skill：`consultation-client-workflow` 1.1.0、`weekly-business-dashboard` 1.1.0。
- 用户自建 Skill：`05_System/Skills/consultant-sop/`，版本 1.0.0，已成功创建。
- 脱敏客户档案：`02_Wiki/01_客户档案/2026-08-05_小A_在职企业管理者客户档案.md`。
- CRM：50号·小A（脱敏验收数据），创建/更新均返回真实成功回执。
- 跟进任务：已真实新增到本周任务；任务按钮耗时约 6 秒，零 Vault 搜索。
- 咨询简报：生产修复后成功生成 `04_Output/AI霖子输出/客户咨询简报/2026.08.20_小A（脱敏演示）_客户咨询简报.png`，2,186,007 bytes。
- 经营周报：`04_Output/AI霖子输出/经营周报/2026.08.21_经营周报交互看板.html`，30,379 bytes；真实扫描 39 项、读取正文 21 份，输出路径正确。
- Studio 自建 Skill：`05_System/Skills/note-to-seven-day-action-course/`，5 个文本文件，manifest 为 `1.0.0`，输出方式为 `create-note`；创建和安装成功，最后一次 AI 试运行被 19 分余额门槛拦截。
- ZIP 导入：原版 `methodology-extract` 因缺少 AI霖子 manifest 被拒绝安装；兼容版 `methodology-extract-course` 校验并安装成功，随后只修改课堂副本的一条自动调用短语。

## 自动化验收

- 插件 `npm test`：通过。
- 插件 marketplace readiness：扫描 54 个源文件，通过。
- 插件 TypeScript + production build + bundle load：通过。
- 看板真渲染测试：只生成 3 个“今日”复选框，“昨日”复选框为 0；无网络依赖。
- WebApp：定向回归、ESLint、TypeScript、Next production build 通过。
- 2026-08-21 再跑 `npm test`：全部通过，市场扫描 54 个源文件通过。
- `npm run package:release`：成功；`dist/ai-linzi.zip` SHA-256=`9546b9f13b67ead7d89bd4c7d01397beea469d0c233fa1d212ed64ef45b036a6`。
- 构建 `main.js` 与数字大脑安装包逐字节一致，SHA-256=`78aeb3d9d4fe30041d91a2da79df9419f9036fab87f66105aa2be632125f0c4e`。
- 飞书 D5 已按 5 个模块补齐 20 张截图；复核删除首尾误插入的 `fragment` 包装段后 revision=`3946`。

## 发布门禁

1. Alina 明确处理课堂演示账户积分后，解锁 Mac，使用现有插件密钥运行 `note-to-seven-day-action-course`，确认只读当前笔记、生成完整 Markdown 预览并写入 `$OUTPUT/行动计划/`。
2. Alina 本人快速验收 Skill Studio 话术、两项官方 Skill 与上述自建 Skill；Codex 验收不能代替真实用户验收。
3. 若代码没有再变，复用本次全量测试与发布包哈希；若有任何代码变化，必须重新运行全部门禁并重建发布包。
4. 只有完成上述真人验收后，才更新 WebApp `LATEST_PLUGIN_VERSION`，创建纯版本号 `0.7.69` 标签/GitHub Release，并按 Obsidian 官方市场机制发布。

## 尚未实现

“一次授权，自动完成整条咨询闭环”目前只是已确认的产品方向，尚未进入 0.7.69。现候选仍是分步确认；任务按钮已经具备直达云端写入的底层能力。推荐后续提供两种模式：默认“一键完成本次流程”（一次展示全清单并确认），可切换“每步确认”。不得做永久无条件控制整个 Vault 的总开关。
