# 0.7.71 界面重设计 · 回交给 Codex 审核

> 回交人：Claude Code　日期：2026-08-21
> 格式按《给 Claude Code 的 0.7.71 界面开发交接》§12 的八项要求
> **状态：已停止写仓库，等待 Alina 与 Codex 审核。未合并、未打标签、未发布、未动 WebApp latest。**

---

## 1. 分支与最终 commit

- 分支：`feature/0.7.71-ui-redesign`（已推送到 origin）
- 最终 commit：见文末「回交状态」一节（本文档随最后一个 commit 一起更新）
- 基线：`98c4f54`（0.7.70 运行时代码）

commit 列表，按交接文档 §8 的要求把「纯视觉壳」与「`/` 面板」拆开，方便单独回滚：

| commit | 内容 |
|---|---|
| `9ab51a1` | P0 纯视觉重组（顶栏 / 入口收纳 / composer / 消息层级 / 空状态 / 活动流折叠 / 响应式） |
| `16aff06` | P1 `/` 命令面板 |
| `740bab3` | Codex 首轮审核两条修复（`/` 面板菜单去重、补品牌资产检查） |
| `9cb536b` | 品牌视觉重做起手式 + 顶栏账号行 + **选择器权重铁律** |
| `3675a25` | **属性声明铁律**（起手式两行文字被主题固定高度裁掉） |
| `d56b07b` | 起手式收成一行大字 |
| `d5fc051` | 本回交文档（首版） |
| `5cbb9da` | 确认卡动作行按钮补间距 |
| `baf3962` | CSS 权重注释与实际 (0,3,1) 对齐（Codex 首轮阻断项） |
| `3ed89bd` | **历史弹窗功能改动**（首轮审核范围之外，见 §2.5） |
| `de65320` | Codex 二轮意见：条数排除过程记录 + 标记口径收窄 + 本文档更新 |
| `1276569` | **Codex 最终审核意见**：标题统一推导（保存前生切根治）+ 文档清理 |

---

## 2. 精确改动文件清单

相对 `98c4f54`，共 **21 个文件、+2730 / −164 行**。构成：

| 类别 | 个数 | 说明 |
|---|---|---|
| 运行时代码 | 6 | `src/` 5 个 + `styles.css` |
| 测试与门禁 | 9 | `scripts/` 8 个 + `package.json` |
| 文档 | 2 | 本回交文档 + 原交接文档（追加附录 A） |
| 归档资源 | 4 | `docs/assets/0.7.70-baseline/` 四张基线截图 |

> ⚠️ 下面两张表只列**运行时代码与测试门禁**（15 个），不含文档与归档资源。

**运行时代码（6 个文件，+1374 / −153）**

| 文件 | 改动 |
|---|---|
| `src/main.ts` | +507 / −… 顶栏重建、composer 重组、空状态两态、活动流折叠渲染、`/` 面板、账号行、历史行渲染 |
| `styles.css` | +671 / −… 品牌 token、权重体系与属性 reset、composer 卡、起手式、活动流折叠、容器查询断点、历史行元信息 |
| `src/brand-assets.ts` | **新增** 官方 Q 版头像（96×96 内联 data URI）+ ribbon 图标 SVG |
| `src/slash-menu-core.ts` | **新增** `/` 触发判定纯模块 |
| `src/activity-feed-core.ts` | +28 新增 `parseFinishedActivityFeed()` 纯函数（仅解析，不碰记录/去重/历史） |
| `src/history-entry-core.ts` | **新增** 历史行展示信息纯模块（相对时间 / 条数 / 标题截断 / 整理方案标记） |

**测试与门禁（8 个文件 + package.json）**

| 文件 | 改动 |
|---|---|
| `scripts/test-button-feedback.mjs` | +169 入口存在性判定升级 + 三组新不变量 + **两条 CSS 铁律守卫** |
| `scripts/test-chat-actions.mjs` | +45 入口断言与承载形态解耦 + `/` 面板去重回归 |
| `scripts/test-activity-feed-core.mjs` | +48 折叠解析的真跑行为测试（第 9 组） |
| `scripts/test-slash-menu-core.mjs` | **新增** `/` 触发判定行为测试（5 组，覆盖 4 个真实边界） |
| `scripts/test-brand-assets.mjs` | **新增** 头像格式/尺寸/体积/ribbon 一致性（解码回二进制验） |
| `scripts/test-history-entry-core.mjs` | **新增** 历史行信息行为测试（6 组） |
| `scripts/test-marketplace-readiness.mjs` | +64 构建产物动态 `<script>` 扫描 + **动态代码生成基线** |
| `scripts/test-production-bundle-load.mjs` | +4 obsidian 桩补 `addIcon` |
| `package.json` | 接入三个新测试（slash / brand-assets / history-entry） |

**归档（不影响运行时）**：`docs/assets/0.7.70-baseline/` 四张基线截图。

### 2.5 ⚠️ 历史弹窗已发生功能改动（首轮审核范围之外）

首轮审核通过之后，Alina 追加要求并批准了历史弹窗的改动。**这部分未经 Codex 审核过，请重点看。**

**改了什么**

| 项 | 说明 |
|---|---|
| 相对时间 | 「刚刚 / N 分钟前 / N 小时前 / N 天前」，超 30 天回到绝对日期；绝对时间移入 `title` |
| 正文条数 | 只统计 user/assistant 的**正文**往返；活动流与技能进度条（`localSkillStatus`）**不计入** |
| 「执行过整理方案」标记 | 见下方口径说明 |
| 「出过图」标记 | 来自 `aiImageResult` / `imageResult` |
| 标题截断 | 见下方「标题统一推导」 |

**数据来源与口径（这是本节最需要审核的部分）**

- 所有事实**只来自本机会话副本**（`SavedConvo.messages`）。纯云端对话读不到副本时，**一条元信息都不显示**——宁可不显示，也不编造条数。
- 「执行过整理方案」的唯一信号是 `WireMessage.vaultActionId`。经核查，该字段**只在 `applyVaultPlan()` 成功后写入一处**（`src/main.ts` 的整理方案确认卡）；**新建笔记、新建文件夹、局部改写等写入动作都不写它**。
  因此标记文案与 tooltip 都严格限定为「执行过整理方案（移动 / 重命名）」，**不使用「动过文件」这类泛化说法**。
  > 本轮**刻意不**为「所有写入动作」建立统一追踪 —— 那需要新增持久化字段并改动多处写入路径，属于独立车次，不在 0.7.71 范围内。
- **未新增任何持久化字段**，未改 `SavedConvo` / `WireMessage` 的结构，未新增 API 调用。

**标题统一推导（二轮审核后补修）**

首版只在渲染层调 `truncateTitle()`，但标题**在保存时就已经被 `slice(0, 24)` 生切**，
字符串里没有「它被截过」的信息，渲染层补省略号来不及——真机仍显示
「从 Skill Studio 安装「consul」。Codex 二轮指出，现已统一：

- 新增 `deriveConversationTitle()`：从**首条用户正文**取第一段有内容的行，
  上限 **40 码点**（旧为 24），超出时在**同一处**补 `…`。
- 三个调用点统一走它：本机保存 `persistNow()`、云端会话转换、历史展示。
- 历史展示**优先用本机会话正文重新推导**，因此 0.7.71 之前存下的旧记录
  也能恢复成带省略号的完整标题；没有本机副本时退回已存标题。
- CSS 侧的 `min-width: 0` 修复保留（flex 行里没有它，`text-overflow` 永不生效），
  但它已不是省略号的唯一依赖。

**未做**：可编辑对话标题。它需要新增本地标题覆盖表（持久化字段），已与 Alina 确认排到后续版本。

逻辑在 `src/history-entry-core.ts` 纯模块，由 `scripts/test-history-entry-core.mjs` 六组真跑测试覆盖。

---

## 3. 前后对比

- **0.7.70 基线**：`docs/assets/0.7.70-baseline/` 四张（320/600 × 浅/深），由 Alina 在真机截取
- **0.7.71 候选**：Alina 已在验收库真机截图确认（顶栏账号行 `Ext.00584 · 10,496 积分` 正常、ribbon Q 版头像正常、起手式与 composer 正常）

⚠️ **我自己出的对比图是 CSS 渲染台，不是真机截图**，见第 5 节说明。

---

## 4. 自动化命令与结果

在最终代码 commit（见文末「回交状态」）上重新跑过，取的是真实退出码：

```
npm test                   → 退出码 0
npm run build              → 退出码 0（tsc --noEmit + production esbuild）
npm run check:marketplace  → 退出码 0（eslint --quiet + 59 源文件扫描 + 产物扫描）
逐个跑 scripts/test-*.mjs  → 57 / 57 全部通过
```

`check:marketplace` 新增两条输出：
- `已扫描构建产物 main.js：动态 <script> 字面量 0 处`
- `动态代码生成：产物 4 处（基线 4，全部来自第三方依赖），本仓库源码 0 处`

**测试升级说明（对应交接文档 §9「不得机械放松」）**

两个测试因入口改名/改形态而失败，均按「升级为更强不变量」处理，未放宽断言强度：

- `test-button-feedback.mjs`：按钮文字清单改为「入口存在且用户能读到它的名字」（三种承载：文字按钮 / `aria-label` / 菜单 `setTitle`），同时**新增五组更强的断言**——可点击入口必须是 `<button>`、图标钮必须带 `aria-label`、窄面板必须走 `@container`、**CSS 权重铁律**、**CSS 属性声明铁律**。
- `test-chat-actions.mjs`：`text: 'CEO驾驶舱'` 与 `cockpitBtn.onclick` 改为断言 `buildWorkbenchMenu` 里「标题正确且接线到 `activateCockpit`」，与承载形态解耦。

两条 CSS 铁律守卫都做过**反向哨兵验证**（故意改坏必须报错、恢复必须通过）。

---

## 5. 真机验收状况（重要，请勿高估）

**我没有做真机验收。** 我的验证手段是：

1. **CSS 渲染台**：用真实 `styles.css` + 代码实际生成的 DOM，在 Chrome 里渲染 320/420/600px × 浅/深，并**模拟 Obsidian 主题的 `button` 规则**（背景、边框、padding、border-radius、height、white-space、line-height）
2. **Chrome 无头计算样式实测**：对 8 类元素逐项 dump `getComputedStyle`，浅色深色两套都验

**Alina 在验收库真机点过的**：面板打开、连接密钥、顶栏账号行、ribbon 与标签页图标、空状态与起手式、composer 外观。她在真机报了两个我渲染台当时抓不到的缺陷，均已修复（见第 7 节）。

**尚未走完的**：`docs`（验收库内）`00_验收指引.md` 的八组清单，特别是这几项**只能真机验**：

- `/` 面板：中文输入法合成期是否误触、上下键/Enter/Escape、先打 `02_知识库` 再敲 `/` 是否正确不弹
- 输入区：中文输入法回车是否误发、图片粘贴与拖入、**拖拽有效区是否覆盖整张 composer 卡**
- 键盘 Tab 到「技能」按 Enter 后，菜单是否贴着按钮弹出（键盘触发无鼠标坐标，走 `showAtPosition`）
- 历史打开/恢复/单删/清空、活动流真实工作流下的折叠与展开
- 深色与高对比主题下的可读性

---

## 6. 有没有改 API / 设置字段 / 权限 / 历史 / 积分 / Skill 协议 / 输出路径

`git diff 98c4f54..HEAD -- src/main.ts` 对相关关键字全量扫描，命中仅两处，均来自新增的顶栏账号行：

| 项 | 结论 |
|---|---|
| **API 路由** | 未新增。账号行读的是**已有的** `/api/plugin/v1/capabilities`，走 `getCapabilities` 既有缓存 |
| **设置字段** | 未增删。只读 `settings.serverUrl` 拼 `/credits` 链接 |
| **权限语义** | 未动。四种授权来源（当前笔记 / 主动附件 / Skill 点名文件 / Vault 工具能力）保持独立；上下文标签只重排 DOM 与样式，`refreshAuthorizedContentUi` 的授权与清除逻辑一行未改 |
| **对话历史** | **结构未动**：`SavedConvo` / `WireMessage` 字段无增删，未新增持久化字段。两处行为变化：① 活动流折叠只发生在**渲染层**，消息正文整条原样入库、展开可完整审计；② 会话**标题的生成方式**改了（统一走 `deriveConversationTitle`，上限 24→40 码点并补省略号），写入的仍是 `SavedConvo.title` 这个既有字段。历史弹窗的展示改动见 §2.5 |
| **积分** | 未动计费逻辑。起手式与 `/` 面板只填输入框或打开已有菜单，不自动发送、不自动执行技能、不产生消耗 |
| **Skill 协议 / 执行** | 未动。`buildOfficialSkillMenu` 只是把原有 `SKILL_ACTIONS` 列表拆成独立一支 |
| **输出路径** | 未动 |
| **历史弹窗** | ⚠️ 有功能改动，但**只读不写**：新增展示信息全部来自已有字段，未新增持久化字段、未改历史结构、未新增 API。详见 §2.5 |

**⚠️ 需要 Codex 特别过目的一处范围外增量**：顶栏账号行（编号 · 积分）与「点积分打开 `/credits`」是 Alina 在开发中途追加的需求，**超出了原交接文档「纯界面车次」的范围**。

具体实现：
- `PluginCapabilities` 新增 `balance?: number` 字段声明（服务端一直在返回，插件此前未声明）
- 面板打开时读一次（走缓存）；**每轮对话结束后 force 刷新一次**（余额已变）——这会比 0.7.70 多出每轮一次 capabilities 请求，是 DB 读取、不经模型、不产生积分消耗
- 读取失败或未连接时**保留原标语**，绝不显示 `0` / `--` 这类会被误读成真实余额的占位
- 充值走 `window.open` 到系统浏览器，插件内不嵌任何支付界面

---

## 7. 未完成项与已知风险

### 7.1 本轮发现的两条 CSS 铁律（建议沉淀进 AGENTS.md）

这两条都是**只有真机才会暴露、原渲染台抓不到**的，各自都已有守卫测试：

**① 选择器权重必须达到 (0,3,1)。** Obsidian 与主题普遍带 `button { padding; border-radius; background }`，深色主题常写成 `.theme-dark button`（0,1,1）甚至更高。首轮实测：单类写法下发送键 `border-radius` 被改成 5px、`padding` 被改成 4px 10px，**根本不是圆的**；深色下三处按钮背景全被刷回灰底。市场检查禁用 important 声明，权重是唯一杠杆，因此统一写成 `.ai-linzi-root <祖先类> button.x`。`@container` 内的规则同样要写足。

**② 权重只能覆盖「声明过的属性」。** 主题 `button` 规则里还有 `height` / `white-space` / `line-height`，我们没声明的那几条，权重再高也拦不住——起手式卡被压成固定 30px 高，第二行说明文字溢出被下一张卡盖住（Alina 截图报障）。现由一条共享 reset 显式声明 `height:auto / min-height:0 / max-height:none / white-space:normal / line-height / font-family:inherit`。

### 7.2 已知风险

- **容器查询**：窄面板断点用 `@container`（`@media` 判的是视口，Obsidian 侧边栏是宽窗口里的窄栏，永远不命中）。需要 Obsidian 的 Electron 支持 CSS 容器查询——minAppVersion 1.11.4 对应的 Electron 版本请 Codex 复核一次。
- **`main.js` 体积**：内联头像使 base64 增加约 19 KB（2937 → 2958 KB）。已由 `test-brand-assets.mjs` 卡 40 KB 上限防止后人塞原图。
- **`addIcon` 用 `<image>` 内嵌 PNG**：语法合法且不含远程地址，但官方市场审核器对 base64 blob 的态度未经实测。建议发版前用「Preview a branch scan」扫一次。
- **验收库 manifest 写的是 `0.7.71-rc.1`**，仓库正式 manifest / package.json 仍是 `0.7.70`，未改。**不要在验收库点「检查并更新」**，市场最新版是 0.7.70，会把候选覆盖掉。
- **紫色用户气泡未调整**，等 Alina 看过真机后决定。

### 7.3 我在本轮犯的两个错误（供审核时警惕）

- 做哨兵验证时用 `&&` 串「改坏 → 测试 → 恢复」，grep 无输出导致链条中断、恢复未执行；随后 `git checkout -- styles.css` 回退，**丢失了未提交的 CSS 改动**。已从渲染台副本逐项核对恢复（5 个关键标记全部比对）。此后所有哨兵验证改为无条件恢复。
- 一次「测试通过」的报告是错的：把 `$?` 取成了管道里 `head` 的退出码而非 `node` 的，实际测试正在崩（JS 的 `RegExp` 不支持 `(?m)` 内联标志）。此后所有测试统计都先跑、再单独取退出码。
- 历史弹窗首版把标记文案写成「动过文件」，而唯一信号 `vaultActionId` 只覆盖整理方案一条路径——**属于过度声称**，由 Codex 二轮审核指出，已收窄（见 §2.5）。同一版还把活动流计入了正文条数，一并修正。
- **声称修好了但只修了一半**：首版标题截断只改了渲染层，没发现标题在保存时就被 `slice(0, 24)` 切掉，于是回交文档写着「已修」而真机照旧生切。由 Codex 最终审核在真机发现。教训：
  「修了某个显示问题」必须回溯到**数据是在哪一步失真的**，只在最后一步补救等于没修。

### 7.4 Codex 二轮意见的落实

| 意见 | 落实 |
|---|---|
| `historyEntryFacts` 排除 `localSkillStatus` | 已改；对应测试断言由 4 改为 3，并补一组「只有过程记录 → 返回 null」 |
| 「动过文件」收窄为「执行过整理方案」，tooltip 同步 | 已改；字段名一并由 `touchedFiles` 改为 `ranVaultPlan`，避免命名继续暗示更宽的口径 |
| 不在本版扩建统一写入追踪 | 未建；已在 §2.5 与源码注释里写明这是独立车次 |
| 更新本文档的最终 commit / 测试数量 / 文件清单，并说明历史功能已改动 | 已更新（§1 §2 §2.5 §4 §6） |

### 7.5 Codex 最终审核意见的落实

| 意见 | 落实 |
|---|---|
| 标题在保存前就被 `slice(0, 24)` 截断，渲染层补救无效 | 新增 `deriveConversationTitle()`，保存 / 云端转换 / 展示三处统一；上限 24→40 码点，超出在同一处补 `…` |
| 历史展示优先从本机正文重新推导，让旧记录恢复 | 已做；无本机副本时退回已存标题 |
| 补一条真实长标题测试 | 第 6 组：直接用报障标题「从 Skill Studio 安装「consultation-client-workflow」」断言必须以 `…` 结尾、长度为 40 码点，并覆盖多行 / 空正文 / 过程记录 / emoji 边界 |
| 文档：测试运行 commit、历史「未动」矛盾、文件数 15→21、`styles.css` 旧文案 | 四条全部已改 |
| 不扩建功能、不拆 `main.ts` | 未做 |

---

## 8. 发布边界

**未做，也不会做**：合并、打 `0.7.71` 标签、创建 Release、抬 WebApp `LATEST_PLUGIN_VERSION`、覆盖 Alina 主库或「数字大脑」演示库。

隔离核验（最终 commit 时）：

| 库 | 插件版本 | 状态 |
|---|---|---|
| `Obsidian大脑`（Alina 主库） | 0.7.61 | 未触碰 |
| `数字大脑`（打卡营演示库） | 0.7.70 | 未触碰 |
| `AI霖子插件验收库`（本轮新建） | 0.7.71-rc.1 | 候选，与仓库构建产物 SHA-256 一致 |

> 附带更正一处交接文档的事实假设：Alina 主库 `Obsidian大脑` 装的是 **0.7.61**，不是 0.7.70；0.7.70 在「数字大脑」演示库里。

---

## 回交状态

- **最终代码 commit**：`1276569`（本分支最后一个含运行时改动的提交）
- **分支 tip**：其后若有提交，仅为本文档回填 commit 号（无运行时改动）
- **测试**：57 个，逐个跑全部通过；`npm test` / `npm run build` / `npm run check:marketplace` 退出码均为 0
- **仓库状态**：已停止写入，工作区干净
- **版本号**：`manifest.json` 与 `package.json` 仍为 `0.7.70`，未改
- **发布动作**：未合并、未打标签、未创建 Release、未动 WebApp `LATEST_PLUGIN_VERSION`

> 另：服务端生图裁切缺陷（3:4 上下共切 11.1%、16:9 共切 15.6%）已在 **webapp 仓库**单独修复并推生产（commit `f46f472`），
> 与本分支无关，插件无需改动即可受益。详见该 commit 说明。
