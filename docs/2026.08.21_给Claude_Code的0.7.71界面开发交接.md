# 给 Claude Code：AI霖子 0.7.71 界面重设计开发交接

> 交接日期：2026-08-21  
> 当前公开基线：0.7.70  
> 目标：在不改变现有能力、权限、数据与后端协议的前提下，把主对话界面改得更清爽、更像一个成熟的 Agent 工作台。  
> 开发分工建议：Claude Code 实现；完成并停止写仓库后，由 Codex 做独立代码审核、回归和真机验收。

---

## 0. 先说结论

这次不要照搬 Ailu，也不要把 AI霖子改成面向开发者的 Codex/Claude Code 容器。

AI霖子的正确方向是：

> Ailu 一样清楚，Codex 一样自主，但普通创业者打开就能用。

Ailu 最值得借鉴的是交互原则：单一对话入口、上下文可见、复杂能力逐步出现、任务过程可追踪。AI霖子的差异化仍然是：商业教练判断、Vault 安全执行、CRM/任务、官方业务 Skill、积分与用户支持。

0.7.71 推荐做成一个低风险的“界面与入口重组版”。不改 API，不改模型路由，不改积分，不改 Vault 工具协议，不改 Skill 执行逻辑，不增加外部依赖。

---

## 1. 开工前必须遵守

1. 完整阅读：
   - `AGENTS.md`
   - `docs/开发上下文.md`
   - 本文
2. `AGENTS.md` 当前比 `CLAUDE.md` 更新，至少多了 0.7.30–0.7.33 的关键边界。不要只读 `CLAUDE.md`；开发时以 `AGENTS.md + docs/开发上下文.md` 为准。
3. 同一时间只能一个 Agent 修改本仓库。Claude Code 开工前先确认 Codex 已停止写入，执行 `git pull` 和 `git status --short --branch`；发现陌生改动立即停下询问 Alina。
4. 从干净的 `master` 建独立开发分支。不要直接发布，不要打标签，不要更新私有 WebApp 的 `LATEST_PLUGIN_VERSION`。
5. 不要复制 Ailu 的代码、CSS、DOM 或素材。Ailu 主体为 AGPL，AI霖子只参考交互思路并独立实现。

---

## 2. 2026-08-21 当前仓库实况（Codex 已重新核对）

### 2.1 Git 与发布

- 仓库：`/Users/alinawang/Desktop/开发项目/AI霖子Obsidian插件`
- 分支：`master`
- 0.7.70 代码基线：`98c4f545f3d4fcb53497802bac82a59a46dbc7b1`（`fix(skills): lock explicitly named vault input`）
- 交接文档提交：`6c2296d`（仅新增本文，运行时代码和 0.7.70 发布资产未改变）
- 本地状态：干净，`master...origin/master`
- 版本：`manifest.json` / `package.json` 均为 `0.7.70`
- Git 标签：`0.7.70` 指向上述运行时代码基线 `98c4f54`；其后的 master 提交仅为交接文档
- GitHub Release：`0.7.70`，非草稿、非预发布，发布于 2026-08-21 04:32:18 UTC
- Release 地址：<https://github.com/AlinaWang321/ai-linzi-obsidian/releases/tag/0.7.70>

当前构建与 Release 资产 SHA-256 一致：

| 文件 | SHA-256 |
|---|---|
| `main.js` | `fe47bc346570c71563b1d70efc4a2e5e3fb8f2363a35d4974f0a93bfb41efae1` |
| `manifest.json` | `214a89dab8268ac892d967d10d98b81709850ee87a4656442c8dc495415ed4eb` |
| `styles.css` | `7b8fc33593b8e17a6df7a36ae607f5605fc0128e1468e30d9cf6f8a799610a31` |

### 2.2 本轮重新运行的质量门禁

以下均在当前 HEAD 重新运行通过，不是引用历史记录：

- `npm test`：通过
- `npm run check:marketplace`：通过
  - ESLint `src --quiet` 通过
  - 扫描 56 个源文件
  - Obsidian 官方市场兼容检查通过
- `npm run build`：通过
  - TypeScript `tsc --noEmit` 通过
  - production esbuild 通过
- `test-production-bundle-load.mjs`：随完整测试通过
- 构建后 Git 工作区仍干净

### 2.3 代码规模与风险提示

- `src/main.ts`：约 6,976 行
- `styles.css`：约 3,552 行
- `src/ + scripts/`：约 112 个文件

主对话 UI、状态机、历史、权限提示和大量确认卡仍集中在 `src/main.ts`。本车次不要顺便大拆文件或重构 Agent 主循环。界面改造尽量限定在 DOM 装配、CSS 和少量纯 UI 辅助函数中。

---

## 3. 0.7.70 已有能力，界面开发不得误判为“尚未实现”

### 3.1 主对话与本地文件

- Luna 在合适的纯文字对话中自主决定是否请求受限本机工具；普通闲聊零预扫。
- 本机工具可搜索、列目录、读取受支持文件；文件改动必须经过结构化预览、本机预检、版本锁和二次确认。
- 当前笔记、主动选择的附件、受限 Skill 点名文件是三种不同授权来源，不能在 UI 上合并成“已授权整个 Vault”。
- 0.7.70 最新修复：明确限制只读当前材料的 Skill，可以根据用户写出的准确路径，只检查路径/文件名元数据并唯一锁定一份文件；歧义时停止，不扩大读取范围。

### 3.2 Skills

- 已有“调用技能”“我的 Skills”“创建 Skill / Skill Studio”。
- 已支持自然语言显式调用、Skill 名称调用，以及无可见面板的 `/技能名` 文本调用。
- 当前没有输入 `/` 后弹出的可视化命令面板。
- Skill 的创建、ZIP 导入、校验、试运行、输出目录和本地程序权限均已有完整安全流程。

### 3.3 图片

- 主对话已经支持自然语言生图、图片附件、图片批次编号、继续修改指定图片和自动保存到 Vault。
- 图片结果卡已有：
  - “继续修改这张”
  - “插入当前笔记”
- 文章配图已有候选预览、插入和重新生成。
- 当前尚无统一的“复制图片”“设为封面”“保存为品牌风格”操作；这些属于后续创作增强，不要在纯 UI 车次伪造按钮。

### 3.4 记忆与知识库

- 用户用自然语言说“请记住”时可以写事实记忆，服务端真实成功后才能回执。
- 用户用自然语言说“存入 AI霖子知识库”时走另一条知识库流程。
- 两者不是同一个目标，文案和 API 不得混用。
- 0.7.26 已明确取消普通回复下方常驻“存为笔记/更新当前笔记”和输入区常驻“存入知识库”。0.7.71 不得因为参考 Ailu 又把这些常驻按钮放回来。

### 3.5 任务过程

- 已有对话内活动流，真实工具动作才出现。
- 工作中显示旋转/呼吸动效；完成后保留最多 12 步，更多步骤显示折叠文字。
- 当前完成态仍以完整状态卡展示，不是可交互收起的摘要。

---

## 4. 当前主对话界面入口

### 4.1 主要代码位置

| 区域 | 文件与位置 | 当前行为 |
|---|---|---|
| ChatView 顶栏与输入区 | `src/main.ts` 约 1922–2037 行 | 长标题、历史/新对话文字按钮、五个常驻入口、独立 textarea 和发送行 |
| 空状态 | `src/main.ts` 约 6206–6243 行 | 未连接三步引导；已连接显示三条技巧；保留网页版入口 |
| 消息渲染 | `src/main.ts` 约 6248–6390 行 | 用户强调色气泡；AI 回复灰色整块；确认卡与结果卡追加在回复下方 |
| AI 图片结果卡 | `src/main.ts` 约 6483–6540 行 | 继续修改、插入当前笔记 |
| 文章配图结果卡 | `src/main.ts` 约 6543–6585 行 | 插入、重新生成 |
| 活动流接线 | `src/main.ts` 约 2897–2951 行 | 本地状态消息，不发送到云端 |
| 活动流纯逻辑 | `src/activity-feed-core.ts` | begin/step/current/end 与 12 行可见上限 |
| 主样式 | `styles.css` | 所有发布样式最终仍打入单一 `styles.css` |

### 4.2 当前视觉问题

1. 顶部是长标题加两个文字按钮，缺少清楚的品牌头部和图标层级。
2. 五个常驻功能按钮占据输入框上方空间，窄侧栏尤其拥挤。
3. textarea、附件、快捷键说明和发送按钮被拆成多块，不像一个统一输入组件。
4. AI 每条回复都铺灰色背景，正文、工作过程和真正需要操作的确认卡层级不够明确。
5. 空状态信息量大，更像说明书，不像“现在可以开始做什么”。
6. 活动流完成后仍然占用较多纵向空间。
7. 授权文件状态已有独立状态条，可以重做成输入框内的上下文标签，但不能改变授权语义。

---

## 5. Ailu 值得参考的部分

官方参考：

- Ailu：<https://github.com/mcncarl/ailu>
- Ailu 完整配置：<https://github.com/mcncarl/ailu/blob/main/docs/COMPLETE_SETUP.md>
- Ailu 第三方许可：<https://github.com/mcncarl/ailu/blob/main/THIRD_PARTY_NOTICES.md>
- Agent Memory Vault：<https://github.com/mcncarl/agent-memory-vault>

值得参考：

1. 一个对话框作为主要操作中心。
2. 输入 `/` 后发现 Skill 和常用入口。
3. 当前文档、选中文字、附件和 Skill 以明确上下文展示。
4. 运行中的任务过程清楚，完成后界面自动安静下来。
5. 图片生成后能顺手进入文章工作流。
6. 记忆写入由用户明确触发，而不是自动吞掉所有聊天。
7. 写作、预览、发布最终形成一条创作流水线。

不要参考或不要在 0.7.71 做：

1. 直接嵌入 Claude Code、Codex 或 CC Switch。
2. 给普通用户暴露模型、供应商、推理等级和 Plan 等开发者设置。
3. 复制 Ailu 代码、样式、图标、品牌配色或小熊猫形象。
4. 引入服务器、CLI、Cookie、Playwright 等完整发布技术栈。
5. 自动沉淀所有对话记忆。
6. 多任务并行、后台恢复和文件写入锁。这是架构车次，不是换界面。

---

## 6. 0.7.71 推荐开发范围

### P0：必须完成的界面重组

#### A. 品牌头部

- 左侧：简洁品牌标识＋“AI霖子”。可以使用 Obsidian/Lucide 已有图标，暂时不要新增远程图片。
- 副标题缩短为“24 小时商业教练”或放在次级行。
- 右侧使用带 `aria-label` 和 tooltip 的图标按钮：历史、新对话、设置。
- 历史与新对话仍调用现有函数，不改变会话命名空间、持久化或删除规则。

#### B. 收起五个常驻按钮

推荐重组为两个入口：

- “技能”菜单：调用技能、我的 Skills、创建 Skill
- “工作台”菜单：内容看板、CEO 驾驶舱

菜单内部必须保留真实用户能理解的完整名称；不能只剩没有说明的图标。

#### C. 统一输入组件

- 把上下文状态、textarea、附件按钮、发送按钮装进一个统一的圆角 composer。
- 现有 `authorizedContentStatusEl` 可以改成 composer 顶部的可删除上下文标签，但只是重排 DOM/样式，不改变内容授权和清除逻辑。
- 保持 Enter 换行、Control+Enter 发送、Mac Command+Enter 兼容。
- 附件仍是最多 3 张图片或现有精确文件授权流程；不得因拖拽外观变化扩大文件授权。
- 发送按钮必须有禁用、发送中和键盘可访问状态。

#### D. 消息视觉层级

- 用户消息保留右侧强调色气泡。
- 普通 AI 正文改成更平的阅读样式，减少整块灰底。
- 确认卡、风险提示、图片结果、Vault 方案和 Skill 追问继续使用卡片，不得与普通正文混成一层。
- Markdown、代码块、表格、长文件名和来源链接必须仍可复制、换行、点击。

#### E. 空状态

- 未连接状态保留现在的三步连接引导和“打开插件设置”。这是功能门禁，不能为了简洁删除。
- 已连接状态缩成一句价值主张＋3 个可点击起手式，例如：
  - 处理当前笔记
  - 扫描知识库完成一项工作
  - 使用一个 Skill
- 保留 `chat.alinalinzi.com` 注册/积分入口，但降低视觉权重。

#### F. 活动流

- 工作中保持自动展开和动效。
- 完成/停止后显示一行摘要，如“已完成 · 8 步 · 36 秒”，详细步骤可以展开查看。
- 不改变 `ActivityFeed` 的动作记录、去重和本机历史边界。
- 不要用纯 CSS 隐藏所有内容导致用户无法审计；完成态必须可展开。

#### G. 响应式与主题

- 至少覆盖 320px、420px、600px 三种面板宽度。
- 覆盖浅色、深色和高对比 Obsidian 主题。
- 尽量使用 Obsidian CSS 变量；品牌强调色只用于关键动作。
- 所有新选择器必须限定在 `.ai-linzi-root` 或插件专属类，不污染其他插件和 Obsidian 编辑器。

### P1：可以同车完成的低风险入口增强

#### `/` 命令面板

输入框为空或光标位于开头时输入 `/`，显示本地菜单：

- 官方技能
- 我的 Skills
- 创建 Skill
- 内容看板
- CEO 驾驶舱

限制：

- 只调用现有函数，不新增服务端能力。
- 选择 Skill 后应填入现有确定性调用短语，或直接复用当前菜单行为；不能静默执行写入。
- 支持 Escape 关闭、上下键移动、Enter 选择、鼠标选择。
- 中文输入法合成期间不误触。
- 不保存用户 Skill 列表到云端。

如果 P0 完成后时间不足，`/` 面板宁可拆到 0.7.72，不要让它拖累主界面稳定性。

### P2：只记录，不在本车次实现

1. Obsidian 选中文字后“问 AI霖子/改写/总结”。
2. 图片“复制/设为封面/保存品牌风格”。
3. 每条回复的非侵入式“记住/沉淀”更多菜单；必须先与 0.7.26 的自然语言产品决定统一。
4. 后台任务队列、多会话并行、任务恢复和文件写入锁。
5. 内容编辑＋公众号/飞书/X 同步预览与发布工作台。
6. 本地持久化增量索引、向量搜索和跨 Agent 共享记忆。

---

## 7. 不可突破的行为边界

1. 不恢复“带上当前笔记”勾选框。
2. 不恢复“智能搜索 Vault”开关。
3. 不把打开当前笔记理解成已经授权读取。
4. 不把“附件”“当前笔记”“Skill 点名文件”“整个 Vault 工具能力”合并成同一种权限。
5. 不增加“完全控制 Vault”的总开关。
6. 不静默创建、移动、覆盖或删除文件；现有确认卡必须保持。
7. 不把本地路径、工具结果、图片数据写入云端历史。
8. 不把事实记忆与 AI霖子知识库混成同一个按钮。
9. 不改变 Starter/Pro/Business 的权益判断；客户端提示不能替代服务端复核。
10. 不直连模型，不在公开插件写模型名、密钥、提示词和计费逻辑。
11. 不增加插件自更新器。
12. 不用 `innerHTML`、远程字体、远程脚本或未经市场检查允许的新 DOM 方式。

---

## 8. 建议的实现顺序

1. 建分支，保存 0.7.70 当前界面截图：宽/窄、浅/深。
2. 只重做头部与功能入口，运行 UI 相关测试。
3. 重做 composer，并逐项验证输入、附件、拖拽、粘贴、快捷键。
4. 重做消息和空状态。
5. 最后做活动流完成态折叠。
6. P0 全部稳定后，再决定是否加入 `/` 面板。
7. 真机截图对比并做窄宽度巡检。
8. 跑完整门禁，提交但不发布，交 Codex 审核。

推荐把“纯视觉壳”和“`/` 面板”拆成两个独立 commit，方便单独回滚。

---

## 9. 自动化验收

至少运行：

```bash
npm test
npm run check:marketplace
npm run build
```

必须确保以下既有测试不因“更新源码字符串”被机械放松：

- `scripts/test-chat-input.mjs`
- `scripts/test-chat-actions.mjs`
- `scripts/test-button-feedback.mjs`
- `scripts/test-chat-scope.mjs`
- `scripts/test-authorized-content.mjs`
- `scripts/test-chat-drop.mjs`
- `scripts/test-activity-feed.mjs`
- `scripts/test-activity-feed-core.mjs`
- `scripts/test-local-skills.mjs`
- `scripts/test-skill-studio.mjs`
- `scripts/test-marketplace-readiness.mjs`
- `scripts/test-production-bundle-load.mjs`

如果 UI 结构变化导致源码契约测试失败，应把测试升级为验证真实行为或新的明确 DOM 契约；不得直接删除保护性断言。

---

## 10. Obsidian 真机验收矩阵

### 空状态

- 未配置密钥：三步引导、打开设置、网页版入口正常。
- 已配置密钥：起手式可见、可点击、不会自动读取 Vault。

### 顶栏

- 历史打开、恢复、单删、清空正常。
- 新对话先保存旧对话，再清空授权与当前状态。
- 设置按钮只打开 AI霖子插件设置。

### 输入区

- Enter 换行。
- Control+Enter 发送。
- Command+Enter 在 Mac 继续兼容。
- 中文输入法回车不会误发。
- 图片粘贴、图片拖入、普通文字粘贴、非图片拖入提示正常。
- 附件超过上限、Starter 权益拦截、长文任务互斥仍正常。

### 功能入口

- 调用技能菜单全部可达。
- 内容看板可达。
- CEO 驾驶舱可达。
- 我的 Skills 菜单与 Skill Studio 可达。
- 菜单键盘操作和 tooltip/aria-label 正常。

### 消息与卡片

- 普通长文、标题、列表、引用、表格、代码块显示正常。
- 来源文件长名称可换行、可点击。
- Vault 整理方案、写入确认、CRM 差异、Skill 创建、Skill 追问、图片结果、长文进度仍明显区别于普通正文。
- 活动流工作中展开；完成后收起；展开后步骤完整。

### 尺寸与主题

- 320px、420px、600px 面板宽度无溢出、截断和按钮相撞。
- 浅色/深色主题均可读。
- `prefers-reduced-motion` 下无强制呼吸或旋转动画。

---

## 11. 发布边界

本车次完成后只能说：

- 已实现
- 本地自动化已通过
- Obsidian 真机 UI 已验收（如果确实跑过）

在 Alina 明确确认前，不能：

- 合并并打 `0.7.71` 标签
- 创建 GitHub Release
- 抬私有 WebApp latest
- 宣称已上架 Obsidian 市场
- 覆盖普通用户或 Alina 主库现有版本

建议先把候选安装到单独测试 Vault，保留 0.7.70 备份；确认 UI 后再走正式发版门禁。

---

## 12. 开发完成后的回交格式

Claude Code 请回交以下内容，方便 Codex 独立审核：

1. 分支与最终 commit。
2. 精确改动文件清单。
3. 0.7.70 前后对比截图：浅/深、320/420/600px。
4. 自动化命令与完整结果摘要。
5. 真机实际点过的验收项目。
6. 明确说明有没有改 API、设置字段、权限、历史、积分、Skill 协议或输出路径。
7. 未完成项和已知风险。
8. 不要发布；等待 Alina 与 Codex 审核。

---

## 13. 后续路线，不要混进 0.7.71

- 0.7.72：选中文字问 AI霖子、图片创作后操作、非侵入式记忆/知识沉淀入口。
- 0.8.0：后台任务队列、多会话状态、安全写锁和任务恢复。
- 后续内容工作台：编辑与预览同步滚动，公众号优先，飞书/其他平台按真实需求接入。
- 长期：本地增量索引与缓存，降低大 Vault 反复读取成本；是否使用向量检索需要单独评估隐私、跨平台和积分模型。

---

# 附录 A：Claude Code 的回执与补充

> 追加人：Claude Code　追加日期：2026-08-21　追加时基线：`9f28d2e`（工作区干净，与 `origin/master` 同步）
> 目的：把 Claude Code 独立做的 Ailu 源码对标结论并入本文，与 Codex 完成认知对齐。本附录不改动第 1–13 节任何结论。

---

## A.1 回执：本文第 1–13 节的约束，全部接受

Claude Code 在读到本文之前，已独立做过一轮 Ailu 对标（读取 GitHub 上 `mcncarl/ailu` 的完整文件树、README 与关键模块，逐项比对本地源码）。两边独立收敛到的结论高度一致：

| 议题 | Codex 结论 | Claude Code 独立结论 | 是否一致 |
|---|---|---|---|
| 是否照搬 Ailu | 不照搬，路线不同 | 不照搬，路线不同 | ✅ |
| 品牌头部 | 标识＋名称＋副标题，右侧图标钮 | 同 | ✅ |
| 五个常驻按钮 | 收成「技能」「工作台」两个菜单 | 收成两个菜单，**分组完全相同** | ✅ |
| 输入区 | 统一圆角 composer，状态条改上下文标签 | 同 | ✅ |
| 空状态 | 未连接保留三步引导，已连接精简 | 同 | ✅ |
| 样式作用域 | 限定 `.ai-linzi-root` | 同（另已核实爆炸半径，见 A.4） | ✅ |
| 选中文字提问 / 图片设为封面 | 排到 0.7.72 | 认为价值高但同意延后 | ✅ |
| AGPL / 本机 CLI / 自建服务器 / 多会话并行 | 不抄 | 同 | ✅ |

**以下三条是 Codex 文档独有、Claude Code 原方案未覆盖的，现明确采纳为开发约束：**

1. **§3.4 的 0.7.26 产品史约束。** 常驻「存为笔记 / 更新当前笔记 / 存入知识库」已在 0.7.26 明确取消并改为自然语言触发。Claude Code 原 UI 方案未包含这段历史，设计统一 composer 时存在把常驻按钮加回去的真实风险。**采纳：0.7.71 不得新增任何常驻写入类按钮。**
2. **§7.3 / §7.4 的授权语义边界。** Claude Code 原方案画的上下文标签（`📄 xxx.md ×`）在视觉上会被读成「这篇笔记已授权给 AI 读取」，而产品语义上**打开 ≠ 授权**，且当前笔记 / 主动附件 / Skill 点名文件 / Vault 工具能力是四种独立授权来源。**采纳：标签只做 DOM 与样式重排，不改 `refreshAuthorizedContentUi` 的授权与清除语义；标签文案须能区分「已授权」与「当前打开」。**
3. **§6 P0-D 消息视觉层级与 P0-F 活动流折叠。** Claude Code 原方案只审了顶栏与底栏，未审消息区。经复核 `activity-feed-core.ts:26` 确认 `ACTIVITY_FEED_VISIBLE_LINES = 12`，完成态确实长期占用纵向空间。**采纳，且认为 P0-D 对观感的影响不低于底栏改造。**

---

## A.2 补充：三个执行细节，建议并入 §6 与 §9

以下三条是复核源码时发现的具体陷阱，本文尚未点名，建议开发时逐条防守。

### A.2.1 拖拽与粘贴绑定在 `footer` 上，改结构会移动命中区

`src/main.ts:2010` 是 `this.registerAttachmentDropAndPaste(footer)`，实现在 `src/main.ts:2460`。P0-C 把 `footer` 改成统一 composer 卡片后，**拖拽的有效命中范围会随 DOM 结构一起变化**。

- §10 验收矩阵里的「图片拖入」能事后抓到，但属于末端拦截。
- **建议：** 改 composer 时显式确认 drop/paste 的绑定宿主元素仍覆盖整个输入卡片（含上下文标签区与控制条），而不是只覆盖 textarea。绑定点变更须在回交清单中单独说明。

### A.2.2 §9 的「测试升级」需要写死判定标准，否则会退化成无效放松

§9 的原则正确，但缺少可判定的下限。实际执行中最容易出现的退化是：

```
// 改前（无效）
assert.match(main, /cls: 'ai-linzi-send'/)
// 改后（同样无效，但看起来像"升级了"）
assert.match(main, /ai-linzi-send/)
```

**建议把标准写死为二选一：**

- **要么**升级为真实行为断言：构造 DOM、触发事件、断言结果（例如禁用态下点击不发送、Control+Enter 触发一次 `send()`）。
- **要么**保留字符串断言，但必须在文件头加一行注明：`// DOM 契约测试：只保证选择器存在，不证明行为正确`，并在本文 §13 后续路线里登记为待补的行为测试。

**不接受**只放宽正则、保留原有断言强度的第三种改法。

### A.2.3 新入口必须仍是 `<button>` 元素，否则按压反馈静默失效

`installButtonPressFeedback(root)`（`src/main.ts:1920`）与 `styles.css` 的 `.ai-linzi-root button:active` / `.is-ai-linzi-pressed` 共同提供全面板按压手感。

P0-B 把五个按钮改成「无边框文字 ＋ ⌄」的下拉入口时，很容易顺手改成 `<div>` 或 `<span>`——**按压反馈会静默消失，且不报错**。`scripts/test-button-feedback.mjs` 已在 §9 保护清单中会变红，但失败信息不会直接指向根因。

**建议：** 在 §6 P0-B 补一句硬约束——所有可点击入口必须是 `<button>` 元素，视觉扁平化只通过 CSS 实现。

---

## A.3 一处分歧及结论：`main.ts` 的拆分

**Claude Code 原主张：** 借本次 UI 改造顺手把 `ChatView` 从 `main.ts`（6,976 行 / 298 KB，占全部源码约三分之一）抽到独立文件。

**Codex 主张（§2.3）：** 本车次不拆文件，界面改造限定在 DOM 装配、CSS 和少量纯 UI 辅助函数。

**结论：采纳 Codex，本车次不拆。** 理由是 Claude Code 原判断未考虑到的两点：

1. UI 改版与文件重构混在同一批 commit 中，出问题时无法二分定位。
2. 现有 UI 相关测试多为源码字符串断言，抽文件会让一批测试同时变红，**红的原因会与 UI 改动的红混在一起，无法区分**。

**但保留一条意见：** 拆分应作为独立车次尽快单独立项（建议 0.7.72 或 0.8.0 之前），不宜无限期后置。理由：单文件 7,000 行意味着任何维护者（含 AI）都无法把它完整读入上下文，只能盲改局部——这是「改一处崩三处」与「修好的问题下一版复发」的机械成因，而非风格问题。

---

## A.4 已核实的事实，供开发时直接引用

以下均为 Claude Code 在本地仓库与 GitHub API 上实际核过的数据，非推测：

**样式爆炸半径（决定 P0 的改动安全性）**

- `ai-linzi-root`、`ai-linzi-footer`、`ai-linzi-action-btn`、`ai-linzi-send*` 全库 grep 仅对话面板使用，**改这些类不会波及内容看板、CEO 驾驶舱、小红书卡片与 Skill Studio**。
- 唯一跨面板选择器是 `.ai-linzi-root button:active` 与 `.ai-linzi-history-modal button`（共用按压反馈）。**这两条不要改。**

**现存的具体视觉缺陷（可作为 P0 的验收对照）**

- `.ai-linzi-actions` 是 `grid-template-columns: repeat(3, 1fr)` 装 5 个按钮 → 第二行恒为「2 个 ＋ 1 个空位」。
- `.ai-linzi-action-btn` 为 `padding: 4px 3px` ＋ `white-space: nowrap` ＋ `text-overflow: ellipsis` → 320 px 面板宽度下「CEO驾驶舱」已被截断。
- `.ai-linzi-authorized-content-status` 使用硬编码 `rgb(0 87 255 / 24%)` 与 `rgb(0 87 255 / 6%)`，`.ai-linzi-long-document-*` 使用硬编码 `#0057ff` → **深色主题下为发光蓝斑**，与 §6 P0-G「尽量使用 Obsidian CSS 变量」直接冲突，建议本车次一并换成 `var(--interactive-accent)` ＋ `color-mix()`。

**Ailu 侧的客观数据（仅供参考，不构成照抄依据）**

- 仓库创建于 2026-08-14，版本 `0.2.0`，88 star，AGPL-3.0，从 WeSight 0.4.0 派生。
- **不在 Obsidian 官方社区市场**（已查 `obsidianmd/obsidian-releases` 的 6,816 条目录，无 `ailu`）；AI霖子以 `ai-linzi` 在册。其安装方式为手动下载三个文件或源码自建部署。
- `src/` 分 14 个模块目录、142 文件；`tests/` 105 个 vitest 行为测试，测试代码量约为源码的 0.54 倍。
- 完整功能依赖用户自备：固定 IP 服务器、HTTPS 入口、Python/Playwright、`lark-cli`、已登录的 Claude Code 或 Codex。其安装文档 25 KB。

---

## A.5 本车次范围外、但需要单独立项的三件事

以下三条不属于 0.7.71，**此处仅作登记，避免掉在两个 Agent 的交接缝里**。是否立项由 Alina 决定。

### A.5.1 发布链路缺少「回读核验」与幂等键

当前公众号直发（`src/publish.ts`）在拿到 `media_id` 后即写入笔记 frontmatter 并结束，**没有回读核验，也没有幂等键**。网络抖动时无法确认草稿是否真的建成；重试可能产生重复草稿。

Ailu 在同一场景的三条纪律值得逐条对照采纳：① 上传前本机预检（去重复标题与封面、清理危险列表结构、逐张检查图片、超限正文图压至 1 MB 内）；② 创建后回读核验；③ **回读失败时保留已返回的 `media_id` 并提示人工核对，绝不自动重试**，端到端防重复依赖 `Idempotency-Key`。

建议把「先预检 → 再执行 → 后核验 → 失败只报告不重试」作为所有写外部系统动作的统一形状：公众号、飞书、CN 网站导入。

### A.5.2 测试范式的系统性问题（比本车次的局部麻烦更大）

§9 已正确处理了「本车次会碰到的那几个测试」，但问题是系统性的：

- 54 个测试中 **11 个是纯源码正则断言**（读取 `.ts` 源文件字符串，不 import、不执行任何代码）：`test-ai-image-flow`、`test-activity-feed`、`test-api-versioning`、`test-chat-actions`、`test-chat-input`、`test-chat-image-media-type`、`test-friendly-error`、`test-illustration-resume`、`test-production-bundle-load`、`test-settings-migration`、`test-search-and-write-truth`。
- `npm test` 是一条 54 段的 `&&` 串行链：**任一环节失败，其后全部不执行**，既看不到全貌，也无法单独重跑某一项。

这类测试对行为回归天生无效——同一类问题反复复发（例如历史上同类 stall 连报四次），机械成因即在此。建议单独立项评估迁移到测试运行器（vitest 等），优先补对话主链与发布主链的真实行为测试。

### A.5.3 上架风险的真实来源是构建产物扫描，不是 LICENSE

**先更正一条过时结论。** 本附录初稿曾写「专有 LICENSE 与官方市场冲突尚未处理」，该说法来自 2026-07-28 的旧审核笔记，**已经过时，事实相反**：

- `LICENSE` 早在 2026-07-27 的 commit `6f16810`（`Fix Obsidian marketplace automated review (#19)`）中，就已从专有协议改为 **MIT**。
- 公开仓库 `AlinaWang321/ai-linzi-obsidian` 为 **public**，`src/` 下 **56 个源文件全部公开**。
- 因此**协议层不存在待处理冲突**，0.7.71 与后续车次都不需要为此做任何改动。

**真正导致过下架的是另一件事，且目前仍未堵住。**

2026-08-16 官方目录对 0.7.33 自动审核判 Failed，唯一 Error 位于 **CODE OBFUSCATION**：`Found 4 dynamic <script> element creations`。根因是 `docx@9.7.1` 的 dist 内联了 `jszip → lie → immediate` 与 `setimmediate` 依赖链，两个 IE 时代 polyfill 各含 2 处动态 `<script>` 调度兜底（Electron 中永不执行的死代码），扫描器按字面量计数。与协议无关，与源码也无关——**这些字符在 `src/` 里一个都没有，是打包后才进入 `main.js` 的**。

**当前硬闸抓不到这类问题：**

`scripts/test-marketplace-readiness.mjs` 的 `createElement` 检查只遍历 `src/*.ts`（0.7.55 已从 8 个文件扩到全量 56 个，但扩的是**源码覆盖面**，不是**扫描对象**）。它从未读取构建产物 `main.js`。如果下一个新增依赖的 dist 里再内联一段类似 polyfill，这条闸仍然是绿的，下架会重演。

**建议的最小补丁（十几行，一次做完长期有效）：**

在 `test-marketplace-readiness.mjs` 末尾增加一条产物断言 —— 若 `main.js` 存在则读取，统计 `createElement("script")` / `createElement('script')` 字面量出现次数，非 0 即 fail 并打印命中片段；`main.js` 不存在时打印跳过提示而非静默通过。同时把「新增第三方依赖前必须扫其 dist 是否内联动态 `<script>`/`eval` 类 polyfill」写进 `AGENTS.md` 的依赖新增流程。

**本车次范围：** 0.7.71 是纯界面车次，不新增任何第三方依赖，因此不受此风险影响，也不在本车次修这条闸。此处仅登记，建议随下一个会动依赖的车次一并补上。

**另一个可选议题（非风险，供 Alina 日后考虑）：** MIT 是最宽松的协议，允许任何人 fork 插件外壳后闭源商用。考虑到 AI霖子的核心价值在服务端、插件外壳脱离服务端无法工作，实际风险很低；若日后希望约束 fork 行为，可评估改为 AGPL-3.0（Ailu 即采用此协议）。**这是商业选择，不是上架要求，不影响任何车次。**

---

## A.6 分工与顺序（Claude Code 的建议）

- 采纳本文卷首的分工：**Claude Code 实现 → 停止写仓库 → Codex 独立审核与真机验收**。
- 顺序上建议**先完成本附录的文档对齐（即本次提交），再开工写代码**：文档是两个 Agent 的契约，先落纸能让方向性误解在动代码之前暴露，而不是改完两千行才发现边界理解错了。本次提交同时作为「Claude Code 已接管仓库写入」的明确信号。
- 遵守 §8 的实现顺序与 §11 的发布边界：先存 0.7.70 基线截图，纯视觉壳与 `/` 面板拆成两个独立 commit；不打标签、不发 Release、不抬 `LATEST_PLUGIN_VERSION`，等 Alina 与 Codex 双审。
